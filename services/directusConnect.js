// require('dotenv').config();
// const { createDirectus, rest, staticToken } = require('@directus/sdk');

// const BACKEND_URL = process.env.DIRECTUS_URL; // Reemplaza con la URL de tu Directus
// const staticTokenLocal = process.env.DIRECTUS_TOKEN; // Reemplaza con tu token de acceso

// const directus = createDirectus(BACKEND_URL)
//        .with(staticToken(staticTokenLocal))
//        .with(rest({ credentials: 'include' })); // Opcional, para solicitudes con cookies

// export default directus;